// Vendors
import React, { FC } from 'react';

// Locals
import { Header, Title, Wrapper } from '~components';

export const Main: FC = () => (
  <Wrapper>
    <Header>Teste do rasta</Header>
    <Title>My main page</Title>
  </Wrapper>
);
